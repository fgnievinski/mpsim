function out = defrontal (in)
    out = in;
    % this function is called only for variables NOT of frontal type.
end

%!test
%! defrontal([])
%! % whether it runs, that's all it needs to be tested.

